query getStandardsExperienceProduct($key: String!) {
    StandardsExperiencePage(
        where: { _metadata: { key: { eq: $key } } }
    ) {
        items {
            _metadata {
                url { default }
            }
            Product {
                item {
                    ...StandardsProductOCP
                }
            }
        }
    }
}
