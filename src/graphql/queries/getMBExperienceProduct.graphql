query getMBExperienceProduct($key: String!) {
    MBExperienceProductPage(
        where: { _metadata: { key: { eq: $key } } }
    ) {
        items {
            _metadata {
                url { default }
            }
            Product {
                item {
                    ...ProductMBOCP
                }
            }
        }
    }
}
