fragment LandingPage on LandingPage {
    _metadata {
        ...PageUrl
        published
        displayName
    }
    TopContentArea {
        _metadata {
            key
            types
        }
        ...ContentArea
    }
    MainContentArea {
        _metadata {
            key
            types
        }
        ...ContentArea
    }
    SeoSettings {
        ...PageSeoSettingsProperty
    }
    PageAdminSettings {
        ...PageAdminSettingsProperty
    }
}
